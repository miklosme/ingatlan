import { StyleSheet } from 'react-native';

import { COLOR_BACKGROUND } from '../../values';

export default StyleSheet.create({
  root: {
    flex: 1,
    padding: 15,
    backgroundColor: COLOR_BACKGROUND,
  },
});
